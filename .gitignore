# --- MS data + bulk artifacts (canonical on OSC ESS; never committed) ---
# Commit small curated tables as CSV; force-add (git add -f) an intentional parquet.
*.raw
*.mzML
*.mzpeak
*.scanmeta
*.parquet
# Exception: the small curated synthetic-peptide reference ships with the repo.
!protostar/peptides/data/*.parquet

# Data dir holds only small local dev subsets; keep the dir, ignore its contents.
data/*
!data/.gitkeep

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg
*.egg-info/
.eggs/
build/
dist/

# --- Virtualenv / conda ---
.venv/
.env
.envrc

# --- Test / lint / coverage ---
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# --- Jupyter ---
# notebooks/ is an exploration sandbox (not the source of record), but a notebook
# MAY be committed when worth preserving; only checkpoints are ignored.
.ipynb_checkpoints/

# --- Local config (lab-specific; copy from config/osc.example.toml) ---
config/osc.toml

# --- Session / local state ---
session_notes.md
.osc_helper/
# Real example data (MaxQuant exports, etc.) pulled locally for development/planning;
# never committed — committed tests use small synthetic fixtures under tests/data/.
local_examples/
# Claude Code session-local settings + scheduled-task lock
.claude/

# --- Editor / OS ---
.vscode/
.idea/
*.swp
*.swo
.DS_Store
